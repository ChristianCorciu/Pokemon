package fr.effrei.pokemon.repositories;

import fr.effrei.pokemon.models.PokemonArene;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PokemonAreneRepository extends JpaRepository<PokemonArene, String> {
}
